name: ai_proxy
description: DeepSeek API 代理服务器 — 转发 Flutter 请求并注入 API Key
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.11.3

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_cors_headers: ^0.1.5
